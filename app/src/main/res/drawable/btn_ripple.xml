<!-- res/drawable/btn_ripple.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- 系统默认涟漪颜色 -->

    <!-- 关键：使用 shape 定义按钮形状，限制涟漪范围 -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" /> <!-- 与按钮圆角一致 -->
            <solid android:color="@android:color/transparent" /> <!-- 必须是纯色 -->
        </shape>
    </item>

</ripple>