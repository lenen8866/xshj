# 信使汇集_安卓
### 后台版本测试

#### 关于搜索特性
根据用户当前显示模式（中 / 双 / EN）+ 输入内容，自动切换：
当前是 “中”：

搜中文：保持“中”不动；

搜英文：自动切到 “双”（中英对照），方便看到英文所在的上下文。

当前是 “EN”：

搜英文：保持“EN”不动；

搜中文（或中英混合）：自动切到 “双”，让你能看到中文行。

当前是 “双”：不做任何切换。 

所有搜索，需要支持多关键词匹配
目的只有一个：
👉 让用户一定能“看到命中的语言上下文”
#### 软件架构



#### 英文搜索规则
正则表达式里的一个“特殊符号”，叫做 单词边界（word boundary）。我给你用人的话 + 对照表解释清楚👇

一句话解释

\b 表示：
从“字母/数字/下划线（\w）”切换到“非字母/数字/下划线（\W）”的位置。

它不占字符、不显示出来，只是“位置判断”。

在你代码里的含义

你现在英文高亮用的是：

Regex("\\b$keyword\\b", RegexOption.IGNORE_CASE)


意思是：

只匹配 完整的英文单词，而不是单词的一部分。

举几个非常直观的例子

假设关键词是：the

✅ 会被 \bthe\b 匹配的
原文	是否匹配	原因
the	✅	前后都是边界
the.	✅	. 是非字母
the,	✅	, 是非字母
the	✅	空格是非字母
(the)	✅	括号是非字母
❌ 不会被 \bthe\b 匹配的（重点）
原文	是否匹配	原因
there	❌	后面还是字母
theology	❌	是单词的一部分
they're	❌	' 会打断单词结构
the's	❌	's 破坏边界
C++（keyword = C++）	❌	+ 不是 \w
U.S.	❌	. 打断单词
3:16	❌	: 打断单词

👉 所以你看到的现象就是：
“明明搜索命中了，但有些地方不高亮” —— 不是你搜索错，是 \b 拒绝了它。（哪怕是多个关键词）
